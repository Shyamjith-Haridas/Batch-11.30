import 'package:flutter/material.dart';

class ChatTileTwo extends StatelessWidget {
  const ChatTileTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("screen 2"),
      ),
    );
  }
}
