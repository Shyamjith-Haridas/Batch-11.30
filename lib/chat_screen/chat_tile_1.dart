import 'package:flutter/material.dart';

class ChatTileOne extends StatelessWidget {
  const ChatTileOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("data"),
      ),
    );
  }
}
